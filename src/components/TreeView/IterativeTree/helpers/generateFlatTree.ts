/**
 * TODO: Iterative flat tree generator
 */
export const generateFlatTree = (element: Tree) => {
    // if (!element.children) {
    //     return <div>{element.title}</div>;
    // }
    // return element.children.map((children) => <>Children</>);
};
